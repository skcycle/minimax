<Window x:Class="MotionControl.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:MotionControl.App.Views"
        xmlns:converters="clr-namespace:MotionControl.Presentation.Converters"
        mc:Ignorable="d"
        Title="MotionControl - 32轴运动控制系统"
        Height="800" Width="1400"
        Background="#2D2D30"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <converters:BoolToBrushConverter x:Key="BoolToBrushConverter"/>

        <!-- TabControl样式 -->
        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border Name="Border" Background="{TemplateBinding Background}"
                                BorderThickness="0,0,0,2" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#0078D4"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#4C4C4C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#0078D4" Padding="15,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="⚙️ MotionControl" FontSize="20" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                    <TextBlock Text=" - 32轴运动控制系统" FontSize="14" Foreground="#CCCCCC" VerticalAlignment="Center" Margin="10,0,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Ellipse Width="12" Height="12" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#888888"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsConnected}" Value="True">
                                        <Setter Property="Fill" Value="#107C10"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding ConnectionStatus}" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主TabControl -->
        <TabControl Grid.Row="1" Background="#2D2D30" BorderThickness="0">
            <!-- 连接测试页 -->
            <TabItem Header="🔗 连接测试">
                <views:ConnectionTestView DataContext="{Binding ConnectionTestViewModel}"/>
            </TabItem>

            <!-- 轴调试页 -->
            <TabItem Header="🎛️ 轴调试">
                <views:AxisDebugView DataContext="{Binding AxisDebugViewModel}"/>
            </TabItem>

            <!-- 轴监控页 -->
            <TabItem Header="📊 轴监控">
                <views:AxisMonitorView DataContext="{Binding AxisMonitorViewModel}"/>
            </TabItem>

            <!-- 仪表盘页 -->
            <TabItem Header="📋 仪表盘">
                <views:DashboardView DataContext="{Binding DashboardViewModel}"/>
            </TabItem>
        </TabControl>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#0078D4" Padding="10,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Foreground="White" FontSize="12"/>

                <TextBlock Grid.Column="1" Text="{Binding CurrentTime}" Foreground="#CCCCCC" FontSize="12" Margin="20,0"/>

                <TextBlock Grid.Column="2" Text="v1.0.0" Foreground="#888888" FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</Window>
